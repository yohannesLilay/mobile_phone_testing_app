import 'package:flutter/material.dart';

class SubKeyTestScreen extends StatelessWidget {
  const SubKeyTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sub Key Test'),
      ),
      body: Container(),
    );
  }
}
