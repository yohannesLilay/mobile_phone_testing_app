import 'package:flutter/material.dart';

class SpeakerTestScreen extends StatelessWidget {
  const SpeakerTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Speaker Test'),
      ),
      body: Container(),
    );
  }
}
